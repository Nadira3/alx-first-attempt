#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @i: stores the value of the integer
 * Return: 0
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
		return (i);
}
